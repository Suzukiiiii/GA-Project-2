<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=trainer.name%></title>
</head>
<body>
    <h1><%=trainer.name%></h1>
    <ul>
    <% trainer.pokemon.forEach(poke =>{ %>
        
        <li>
            <a href="/pokemon/<%=poke.id%>"><%=poke.species%></a> level: <%=poke.level%>
        </li>
    <% }) %>
    </ul>
    
    <form action = "/trainers/<%=trainer.id; %>?_method=DELETE" method="POST">
        <input type="submit" value="DELETE"/>
    </form>
</body>
</html>